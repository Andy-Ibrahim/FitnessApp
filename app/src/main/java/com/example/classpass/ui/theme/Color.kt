package com.example.classpass.ui.theme

import androidx.compose.ui.graphics.Color

// Primary colors (Facebook Blue style)
val PrimaryGreen = Color(0xFF1877F2)
val PrimaryGreenDark = Color(0xFF0A5CBF)
val PrimaryGreenLight = Color(0xFF5A9EF5)

// Badge colors
val PremiumOrange = Color(0xFFFF8C42)
val LuxuryPurple = Color(0xFFB84FC5)

// Text colors
val TextPrimary = Color(0xFF212121)
val TextSecondary = Color(0xFF666666)
val TextGreen = Color(0xFF1877F2)

// Background colors
val BackgroundLight = Color(0xFFF5F5F5)
val BackgroundWhite = Color(0xFFFFFFFF)
val BackgroundDark = Color(0xFF2B2D31) // NotebookLM exact dark background
val CardDark = Color(0xFF383A40) // NotebookLM exact card background
val TextSecondaryDark = Color(0xFFB9BBBE) // NotebookLM secondary text (timestamps, meta)

// Standard colors
val GrayLight = Color(0xFFE0E0E0)
val GrayMedium = Color(0xFF9E9E9E)
val StarYellow = Color(0xFFFFC107)

// Chat bubble colors
val ChatBubbleUser = Color(0xFF6200EE)
val ChatBubbleAI = Color(0xFFE0E0E0)
val ChatTextUser = Color(0xFFFFFFFF)
val ChatTextAI = Color(0xFF212121)

// Sheet and modal colors
val SheetBackground = Color(0xFF1C1C1C) // Bottom sheet background
val SheetCardBackground = Color(0xFF2A2A2A) // Cards within sheets

// Drawer colors
val DrawerSecondaryText = Color(0xFF9E9E9E) // Secondary text in drawer (labels, metadata)
val DrawerDivider = Color(0x1AFFFFFF) // Divider with 10% white opacity

// Status and indicator colors
val StatusConnected = PrimaryGreen // Connected status (green)
val StatusDisconnected = Color(0xFF9E9E9E) // Disconnected status (gray)
val ErrorRed = Color(0xFFFF4444) // Error/destructive actions (logout, delete)
val ToggleInactive = Color(0xFF9E9E9E) // Inactive toggle switch

